{% extends 'base.html.twig' %}

{% block subtitle %}Szczegóły kosztu{% endblock %}

{% block page_title %}
    Koszty
{% endblock %}

{% block page_subtitle %}
    Szczegóły kosztu
{% endblock %}

{% block page_content %}
    <table class="table">
        <tbody>
            <tr>
                <th>Data powstania kosztu</th>
                <td>{{ outcome.outcomeDate ? outcome.outcomeDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ outcome.description }}</td>
            </tr>
            <tr>
                <th>Kwota</th>
                <td>{{ outcome.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('outcome_index') }}">Powrót do listy</a>

    <a href="{{ path('outcome_edit', {'id': outcome.id}) }}">Edycja</a>

    {{ include('outcome/_delete_form.html.twig') }}
{% endblock %}
